{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "now-mcp": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/scripts/launch.mjs"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
